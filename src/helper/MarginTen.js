import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const MarginTen = () => {
  return <View style={{marginBottom: 20}}></View>;
};

export default MarginTen;

const styles = StyleSheet.create({});
